super + s
	screenshots --now;

super + shift + s
	screenshots --sel

super + alt + s
	screenshots --in10

# Reload sxhkd
super + Escape
	pkill -USR1 -x sxhkd;

# Restart polybar
super + p
    ResetPolybar
