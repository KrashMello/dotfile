* {
    BG:    #f2e9e180;    /* fondo principal oscuro -> beige claro */
    BGA:   #FAF4ED;    /* fondo alterno */
    FG:    #56949f;    /* primer plano, texto principal -> gris violáceo suave */
    FGA:   #575279;    /* primer plano alterno, acentos -> rosa lavanda */
    BDR:   #d7827e;    /* borde suave -> marrón beige semitransparente */
    SEL:   #575279;    /* foreground select (dejado igual para parecido) */
    BGB:   #E6D6B9;    /* fondo seleccionado -> beige medio */
    SELTE: #988BA280;    /* background selection (se mantiene para contraste) */
    UGT:   #DDB6F0;    /* texto destacado -> rosa lavanda */
    IMG:   #BBAE93;    /* fondo imagen, tonos crema oscuro -> marrón claro */
    OFF:   #988BA2;    /* texto o elemento inactivo -> gris lila */
    ON:    #A6D1899F;    /* activo, verde suave mantenido -> verde pastel ligero */
}
